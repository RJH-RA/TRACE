# Security and privacy

Report code vulnerabilities privately to the repository maintainers.

This repository must not contain protected health information or reidentification
keys. If restricted clinical data are committed accidentally, stop work, notify
the data custodian, revoke any exposed credential, and follow the institution's
incident-response process. Deleting a file in a later commit is not sufficient
because Git retains history.
